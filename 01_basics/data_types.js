/*
  DATA TYPES:
  undefined, null, boolean, string, symbol, number and object
*/
var name = 'sandeep';
let rollNumber = 786512;
const PI = 3.14;

// check scope of let, var and const in details

// Strings
let firstName = 'Sandeep';
let lastName = 'Bansal';
let fullName = firstName + ' ' + lastName;
console.log(fullName);
console.log(fullName.length); // String length
console.log(fullName[0]); // First letter of string
console.log(fullName[fullName.length - 1]); // Last letter of a string
